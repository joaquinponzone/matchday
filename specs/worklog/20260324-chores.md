# 2026-03-24 — Project Bootstrap & Core Features

## Commits

- `729bc15` feat: initial commit
- `14b3de6` feat: add initial project setup with environment configuration, agent documentation, and cron job setup
- `5c98063` feat: update cron schedules and enhance notification system with next match functionality
- `a9b0bfa` feat: refactor layout structure, implement new dashboard and notifications pages, and enhance settings management
- `4827cd6` feat: update navigation links with icons, adjust notification filters for full width, and modify cron schedule for daily runs

## Work Done

### Project Setup
- Next.js 16 + React 19 + TypeScript scaffolding
- Tailwind CSS 4 + shadcn/ui (radix-maia) component library
- Turso (SQLite) + Drizzle ORM database setup
- Vercel deployment + cron job configuration
- Environment configuration (.env, CLAUDE.md, AGENTS.md)

### Database Schema
- `settings` — single-row user preferences (email, telegram, timezone, notification timing)
- `matches` — synced fixtures from football-data.org (status, scores, logos)
- `notifications` — sent notifications with idempotency keys
- `followed_teams` — Chelsea FC + Argentina toggles

### Pages & UI
- **Dashboard** (`/`) — next match hero card with countdown, upcoming matches list
- **Upcoming Matches** (`/upcoming-matches`) — full match list with manual sync
- **Settings** (`/settings`) — auto-save preferences, team toggles, channel config, test Telegram button
- **Notifications** (`/notifications`) — history with channel/status filters, mark-as-read
- **Login** (`/login`) — password-only auth with iron-session

### Integrations
- **football-data.org** — fixture sync for Chelsea (ID 61) and Argentina (ID 762)
- **Resend** — HTML email notifications
- **Telegram Bot API** — message notifications with test feature
- **In-app** — stored notifications with unread badge in nav

### Cron System
- Daily cron at 12:00 UTC (`/api/cron/daily`)
- Syncs fixtures + dispatches notifications in one pass
- Idempotency via `{matchId}_{channel}_{timing}` unique keys

## Pending
- Twilio integration (WhatsApp/SMS) — Phase 3
- Match results/scores display after games finish
- Multi-tenant support
- Expanded team selection
