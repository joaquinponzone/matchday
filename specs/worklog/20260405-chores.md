# 2026-04-05 — Notificaciones en popover, refresh en dashboard y feedback de guardado

## Commits

- `56f235b` feat: integrate notification system with popover component and enhance dashboard with refresh button

## Work Done

### UI

- **Notificaciones:** eliminada la página `src/app/(app)/notifications/page.tsx` y el listado con `notification-filters.tsx` / `notification-item.tsx`; nuevo `NotificationPopover` (campana + `Popover` shadcn) con carga de lista, canal y estado.
- **Dashboard:** en `page.tsx`, cabecera “Próximo partido” con `RefreshButton` alineado a la derecha.
- **Navegación:** `nav-links.tsx` actualizado para el nuevo patrón de notificaciones.
- **Ajustes:** `settings-form.tsx` con mejor flujo de guardado; `save-status.tsx` + `use-save-status.ts` para estado guardando/guardado; toque en `settings/actions.ts`.
- **UI base:** añadido `components/ui/popover.tsx`; ajuste menor en `theme-provider.tsx`.

### Hooks

- Sustituido el placeholder `src/hooks/.gitkeep` por `use-save-status.ts`.
